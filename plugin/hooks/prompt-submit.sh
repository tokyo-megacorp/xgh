#!/usr/bin/env bash
# xgh UserPromptSubmit hook
# Static instructions moved to xgh-instructions.md (@reference).
# Context-mode enforcement delegated to context-mode plugin's own hooks.
# This hook is intentionally minimal — exit cleanly with empty output.
set -euo pipefail
echo '{}'
exit 0
